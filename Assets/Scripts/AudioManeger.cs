using System;
using FMODUnity;
using UnityEngine;

public class AudioManeger : MonoBehaviour
{
    [SerializeField] EventReference fostedEvent;
    [SerializeField] float rate;

    // Update is called once per frame
    void Update()
    {
        
    }
}
